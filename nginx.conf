events {}

http {
    server {
        listen 80;

        location / {
            default_type text/html;
            return 200 "<h1>🤫 Ничего нет... но может и есть</h1>";
        }
    }
}
